name: CI
on:
  push:
  pull_request:
jobs:
  pester:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Pester
        shell: pwsh
        run: |
          pwsh -f scripts/Invoke-Tests.ps1
